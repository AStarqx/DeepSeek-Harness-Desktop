/**
 * Public addresses the Desktop shell reports about itself.
 *
 * A build states its own project here, so the About dialog names the repository its releases come
 * from rather than the upstream project it follows.
 */

/** Repository this Desktop build is published from. */
export const DESKTOP_PROJECT_URL = 'https://github.com/Astarqx/DeepSeek-Harness-Desktop'

/** Upstream repository this project follows. */
export const DESKTOP_UPSTREAM_URL = 'https://github.com/deepseek-ai/deepseek-harness'
